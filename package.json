{
  "name": "@nhatphucpham/games-base",
  "private": true,
  "version": "1.0.0",
  "description": "Complete 2D Web Game Engine Platform - Monorepo",
  "workspaces": ["base", "builders", "demo", "game-cli", "server"],
  "devDependencies": {
    "lerna": "^6.0.0"
  },
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "build": "npx lerna run build --stream",
    "test": "npx lerna run test --stream",
    "version": "npx lerna version",
    "publish": "npx lerna publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/AI-Nhat-Phuc/games-base.git"
  },
  "license": "MIT"
}
